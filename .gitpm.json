{
  "system_only": false,
  "dependencies": {
    "system": {
      "method": "yay -S --noconfirm",
      "check_commands": [
        ["waybar", "ironbar"],
        ["docker, podman"],
        "distrobox"
      ],
      "Arch": {
        "waybar": ["waybar", "ironbar"],
        "docker": ["docker", "podman"]
        "distrobox": "distrobox"
      }
    }
  }
}
